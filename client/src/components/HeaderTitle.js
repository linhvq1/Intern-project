import React, { Fragment } from 'react'

function HeaderTitle({text}) {
  return (
        <div className='flex justify-center font-semibold text-2xl mb-4'>{text}</div>   
  )
}

export default HeaderTitle